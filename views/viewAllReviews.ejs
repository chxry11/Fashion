<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>CC.LY - Reviews</title>

  <style>
    body {
      background-color: #f8f9fa;
    }

    .table-container {
      margin-top: 20px;
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .table th, .table td {
      vertical-align: middle;
    }

    .table img {
      width: 70px;
      height: auto;
      border-radius: 8px;
    }

    .edit-btn, .delete-btn {
      display: inline-block;
      padding: 5px 10px;
      font-size: 14px;
      border-radius: 5px;
      text-decoration: none;
      transition: 0.3s ease;
    }

    .edit-btn {
      background-color: rgb(39, 118, 203);
      color: white;
    }

    .edit-btn:hover {
      background-color: #68a482;
    }

    .delete-btn {
      background-color: #f75454;
      color: white;
    }

    .delete-btn:hover {
      background-color: #68a482;
    }

    .text-center h2 {
      color: #070707;
    }
  </style>
</head>
<body>

    <%- include ('partials/navbar.ejs') %>

    <div class="container">
      <div class="text-center mt-4">
        <h2>Reviews</h2>
      </div>

      <div class="table-container mt-4">
        <table class="table table-bordered table-hover text-center">
          <thead class="table-primary">
              <tr>
                <th>Product</th>           
                <th>Review</th>             
                <th>Image</th>     
                <th>Rating</th>  
                <th>Reviewed By</th>           
                <th>Edit</th>
                <th>Delete</th>
              </tr>
          </thead>
          <tbody>
            <% for(let i=0; i < reviews.length; i++) { %>
              <tr>
                <td>
                  <%= reviews[i].productName %><br>
                  <img src="images/<%= reviews[i].productImage %>" alt="Product Image">
                </td>
                <td><a href="/review/<%= reviews[i].reviewId %>" class="text-decoration-none text-dark"><%= reviews[i].reviewContent %></a></td>
                <td><img src="images/<%= reviews[i].reviewImage %>" alt="Review Image"></td>              
                <td><%= reviews[i].reviewRating %> ★</td>
                <td><%= reviews[i].username %></td>
                <td><a href="/editReview/<%= reviews[i].reviewId %>" class="edit-btn">Edit</a></td>
                <td><a href="/deleteReview/<%= reviews[i].reviewId %>" class="delete-btn" onclick="return confirm('Are you sure you want to delete this review?')">Delete</a></td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>
    </div>

</body>
</html>
