<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>CC.LY - Your Ultimate Shopping Hub</title>
  <style>
    body {
      background: #f8f9fa;
      font-family: 'Arial', sans-serif;
    }
    .hero {
      background: url('https://source.unsplash.com/1600x900/?shopping,ecommerce') no-repeat center center/cover;
      color: black;
      padding: 150px;
      text-align: center;
      border-radius: 10px;
    }
    .hero h1 {
      font-weight: bold;
    }
    .card {
      transition: transform 0.3s ease-in-out;
    }
    .card:hover {
      transform: scale(1.05);
    }
    .footer {
      background: #343a40;
      color: white;
      text-align: center;
      padding: 20px;
      margin-top: 50px;
    }
    .highlight {
        background-color: yellow;
    }
    .searchbar {
            width: 250px;
            max-width: 100%;
            border-radius:20px; 
            text-align: center;
        }

   @media (max-width: 768px) {
       .searchbar {
        width: 150px;
        }
    }
    .searchbtn {
        padding: 4px 8px;
        background-color: #8ac6eb;
        color: black;
        border: none;
        cursor: pointer;
        border-radius: 2px;
        margin-left: 10px;
    }
    .searchbtn:hover {
        background-color: #c8e1e6;
    }
  </style>
</head>
<body>

    <%- include ('partials/navbar.ejs') %>

    <div class="container mt-1 d-flex justify-content-end">
      <form action="/search" method="GET" class="d-flex">
          <input type="text" name="q" id="searchInput" placeholder="Search" class="searchbar">
          <button type="submit" class="searchbtn">Search</button>
      </form>
  </div>

    <div class="container mt-4">
      <div class="hero">
        <h1>Welcome to CC.LY!</h1>
        <p>Discover amazing products, read reviews, and shop with confidence.</p>
        <a href="/products" class="btn btn-dark btn-lg">Browse Products</a>
      </div>
    </div>


</body>
</html>
